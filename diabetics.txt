import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.model_selection import train_test_split
%matplotlib inline

df = pd.read_csv('../pima-indians-diabetes-database/diabetes.csv')

df.head()

df.describe()

df.columns

df.info()

sns.countplot(df['Outcome'],)

sns.heatmap(df)

sns.scatterplot(df['BloodPressure'],df['Glucose'],hue=df['Outcome'])

sns.scatterplot(df['SkinThickness'],df['Glucose'],hue=df['Outcome'])

sns.scatterplot(df['BMI'],df['Glucose'],hue=df['Outcome'])

sns.scatterplot(df['Age'],df['Glucose'],hue=df['Outcome'])

sns.scatterplot(df['BMI'],df['BloodPressure'],hue=df['Outcome'])

sns.scatterplot(df['Insulin'],df['BloodPressure'],hue=df['Outcome'])

sns.scatterplot(df['Age'],df['BloodPressure'],hue=df['Outcome'])

sns.scatterplot(df['Age'],df['SkinThickness'],hue=df['Outcome'])

sns.scatterplot(df['BMI'],df['Age'],hue=df['Outcome'])

sns.scatterplot(df['Age'],df['Insulin'],hue=df['Outcome'])

X = df.drop('Outcome', axis=1)
y = df['Outcome']

X_train,X_test,y_train,y_test = train_test_split(X,y)

X_train.head()

y_train.head()

model.summary()

model = load_model('model.h5')
